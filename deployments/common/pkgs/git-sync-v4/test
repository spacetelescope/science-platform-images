#! /bin/bash -eu

cd $COMMON_IMAGE_DIR/pkgs/git-sync-v4

python -m pytest -v -v -v  \
       --tb=short \
       --capture=no \
       --log-level INFO --log-cli-level INFO \
       --doctest-modules \
       --doctest-report cdiff \
       --cov=git_sync_v4 --cov=check_quota --cov-report=html:htmlcov  \
       tests.py tests.txt $*

rm -rf __pycache__
