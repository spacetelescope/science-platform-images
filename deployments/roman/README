Do not directly edit Dockerfile.

Dockerfile is automatically constructed during image-build from
Dockerfile.custom and  ../../common/image/Dockerfile.trailer.

Place all environment variables available to users in the Dockerfile.custom file.
