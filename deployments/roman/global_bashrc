export PS1="${debian_chroot:+($debian_chroot)}\u@notebook:\w\$ "

# Enable conda and activate jwst-masterclass by default
. /opt/conda/etc/profile.d/${CONDA_VER}.sh

source /opt/common-scripts/env-activate roman-cal


